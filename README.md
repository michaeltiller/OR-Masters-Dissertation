DR Model 
- Model can be run through drModelNotebook.ipynb, within the notebook the formulation can also be found
- The code for the model is within drModel.py
- additional functions for plotting the results are in plottingFuncs.py
- To run the model Gurobi is required 
